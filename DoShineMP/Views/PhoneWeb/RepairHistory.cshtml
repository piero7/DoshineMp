
@{
    Layout = "~/Views/Shared/_LayoutQiye.cshtml";
}
@section styles{
    <link href="~/Styles/rest.css" rel="stylesheet" />
    <link href="~/Styles/history.css" rel="stylesheet" />
}
<div id="content">
    <div id="leftTabBox" class="tabBox">
        <div class="hd">
            <ul>
                <li><a href="#">进行中</a></li>
                <li><a href="#">已完成</a></li>
                <li><a href="#">已取消</a></li>
            </ul>
        </div>
        <div class="bd">
            
            @*已提交*@
            @*@if (ViewBag.RepairList5 != null)
            {
                <ul class="Padding">
                    @foreach (DoShineMP.Models.Repair r in ViewBag.RepairList5 as List<DoShineMP.Models.Repair>)
                    {
                    <li id="@r.RepairId">
                        <div class="content-title-t">
                            @if (r.ImageFiles.Count == 0)
                                {
                            <div class="pull-left w10 repaidid" tagid="@r.RepairId" onclick="repairid(@r.RepairId)">
                                <p class="wr-content">@r.Contenet</p>
                            </div>
                                }
                                else
                                {
                            <div class="pull-left w8 repaidid" tagid="@r.RepairId" onclick="repairid(@r.RepairId)">
                                <p class="wr-content">@r.Contenet</p>
                            </div>
                            <div class="pull-right w2">
                                <div class="img">
                                    <img src="@System.Configuration.ConfigurationManager.AppSettings["httpimgpath"]@r.ImageFiles[0].FileName" class="imgyulan" />
                                </div>
                            </div>
                                }
                        </div>
                        <div class="content-title-b">
                            <ins class="check check1 pull-left">已提交</ins>
                            <span class="time pull-right">@r.CreateDate.ToString().Split(' ')[0]</span>
                            @if (r.User.Name != null)
                                {
                            <span class="time-l time pull-right">@r.User.Name</span>
                                }
                        </div>
                    </li>
                    }

                </ul>
            }
            else
            {
                <ul class="Padding">
                    <li style="text-align:center;border:none;padding-top:40%;"><img src="~/Images/weiyou.png" /></li>
                </ul>
            }*@

            @*已受理*@
            @*@if (ViewBag.RepairList10 != null)
            {
                <ul class="Padding">
                    @foreach (DoShineMP.Models.Repair r in ViewBag.RepairList10 as List<DoShineMP.Models.Repair>)
                    {
                        <li id="@r.RepairId">
                            <div class="content-title-t">
                                @if (r.ImageFiles.Count == 0)
                                {
                            <div class="pull-left w10 repaidid" tagid="@r.RepairId" onclick="repairid(@r.RepairId)">
                                <p class="wr-content">@r.Contenet</p>
                            </div>
                                }
                                else
                                {
                            <div class="pull-left w8 repaidid" tagid="@r.RepairId" onclick="repairid(@r.RepairId)">
                                <p class="wr-content">@r.Contenet</p>
                            </div>
                            <div class="pull-right w2">
                                <div class="img">
                                    <img src="@System.Configuration.ConfigurationManager.AppSettings["httpimgpath"]@r.ImageFiles[0].FileName" class="imgyulan" />
                                </div>
                            </div>
                                }
                            </div>
                            <div class="content-title-b">
                                <ins class="check check2 pull-left">已受理</ins>
                                <span class="time pull-right">@r.CreateDate.ToString().Split(' ')[0]</span>
                                @if (r.User.Name != null)
                                {
                            <span class="time-l time pull-right">@r.User.Name</span>
                                }
                            </div>
                        </li>
                    }
                </ul>
            }
            else
            {
                <ul class="Padding">
                    <li style="text-align:center;border:none;padding-top:40%;"><img src="~/Images/weiyou.png" /></li>
                </ul>
            }*@


            @*待评价*@
            @*@if (ViewBag.RepairList20 != null)
            {
                <ul class="Padding">
                    @foreach (DoShineMP.Models.Repair r in ViewBag.RepairList20 as List<DoShineMP.Models.Repair>)
                    {
                    <li id="@r.RepairId">
                        <div class="content-title-t">
                            @if (r.ImageFiles.Count == 0)
                                {
                            <div class="pull-left w10 repaidid" tagid="@r.RepairId" onclick="repairid(@r.RepairId)">
                                <p class="wr-content">@r.Contenet</p>
                            </div>
                                }
                                else
                                {
                            <div class="pull-left w8 repaidid" tagid="@r.RepairId" onclick="repairid(@r.RepairId)">
                                <p class="wr-content">@r.Contenet</p>
                            </div>
                            <div class="pull-right w2">
                                <div class="img">
                                    <img src="@System.Configuration.ConfigurationManager.AppSettings["httpimgpath"]@r.ImageFiles[0].FileName" class="imgyulan" />
                                </div>
                            </div>
                                }
                        </div>
                        <div class="content-title-b">
                            <ins class="check check3 pull-left">待评价</ins>
                            <span class="time pull-right">@r.CreateDate.ToString().Split(' ')[0]</span>
                            @if (r.User.Name != null)
                                {
                            <span class="time-l time pull-right">@r.User.Name</span>
                                }
                        </div>
                    </li>
                    }
                </ul>
            }
            else
            {
                <ul class="Padding">
                    <li style="text-align:center;border:none;padding-top:40%;"><img src="~/Images/weiyou.png" /></li>
                </ul>
            }*@

            @*进行中*@
            @if (ViewBag.RepairListAll != null)
            {
                <ul class="Padding">
                    @foreach (DoShineMP.Models.Repair r in ViewBag.RepairListAll as List<DoShineMP.Models.Repair>)
                    {
                        <li id="@r.RepairId">
                            <div class="content-title-t">
                                @if (r.ImageFiles.Count == 0)
                                {
                                    <div class="pull-left w8 repaidid" tagid="@r.RepairId" onclick="repairid(@r.RepairId)">
                                        <p class="wr-content">@r.Contenet</p>
                                        <div class="content-title-b">
                                            @switch (r.Status)
                                            {
                                                case DoShineMP.Models.RepairStatus.Apply:
                                                    <ins class="check check1 pull-left">已提交</ins>;
                                                    break;
                                                case DoShineMP.Models.RepairStatus.Accept:
                                                    <ins class="check check2 pull-left">已受理</ins>;
                                                    break;
                                                case DoShineMP.Models.RepairStatus.FinishHandle:
                                                    <ins class="check check3 pull-left">待评价</ins>;
                                                    break;
                                            }

                                            <span class="time pull-right">@r.CreateDate.ToString().Split(' ')[0]</span>
                                            @if (r.User.Name != null)
                                            {
                                                <span class="time-l time pull-right">@r.User.Name</span>
                                            }
                                        </div>
                                    </div>
                                    <div class="pull-right w2">
                                        <div class="img">
                                            <img src="~/Images/logo.png" class="imgyulan" />
                                        </div>
                                    </div>
                                }
                                else
                                {
                                    <div class="pull-left w8 repaidid" tagid="@r.RepairId" onclick="repairid(@r.RepairId)">
                                        <p class="wr-content">@r.Contenet</p>
                                        <div class="content-title-b">
                                            @switch (r.Status)
                                            {
                                                case DoShineMP.Models.RepairStatus.Apply:
                                                    <ins class="check check1 pull-left">已提交</ins>;
                                                    break;
                                                case DoShineMP.Models.RepairStatus.Accept:
                                                    <ins class="check check2 pull-left">已受理</ins>;
                                                    break;
                                                case DoShineMP.Models.RepairStatus.FinishHandle:
                                                    <ins class="check check3 pull-left">待评价</ins>;
                                                    break;
                                            }

                                            <span class="time pull-right">@r.CreateDate.ToString().Split(' ')[0]</span>
                                            @if (r.User.Name != null)
                                            {
                                                <span class="time-l time pull-right">@r.User.Name</span>
                                            }
                                        </div>
                                    </div>
                                    <div class="pull-right w2">
                                        <div class="img">
                                            <img src="@System.Configuration.ConfigurationManager.AppSettings["httpimgpath"]@r.ImageFiles[0].FileName" class="imgyulan" />
                                        </div>
                                    </div>
                                }
                            </div>
                           
                        </li>
                    }
                </ul>
            }
            else
            {
                <ul class="Padding">
                    <li style="text-align:center;border:none;padding-top:40%; background-color:#ececec;"><img src="~/Images/weiyou.png" /></li>
                </ul>
            }



            @*已完成*@
            @if (ViewBag.RepairList99 != null)
                                {
                <ul class="Padding">
                    @foreach (DoShineMP.Models.Repair r in ViewBag.RepairList99 as List<DoShineMP.Models.Repair>)
                                {
                        <li id="@r.RepairId">
                            <div class="content-title-t">
                                @if (r.ImageFiles.Count == 0)
                                {
                                    <div class="pull-left w10 repaidid" tagid="@r.RepairId" onclick="repairid(@r.RepairId)">
                                        <p class="wr-content">@r.Contenet</p>
                                    </div>
                                }
                                else
                                {
                                    <div class="pull-left w8 repaidid" tagid="@r.RepairId" onclick="repairid(@r.RepairId)">
                                        <p class="wr-content">@r.Contenet</p>
                                        <div class="content-title-b">
                                            <ins class="check check4 pull-left">已完成</ins>
                                            <span class="time pull-right">@r.CreateDate.ToString().Split(' ')[0]</span>
                                            @if (r.User.Name != null)
                                            {
                                                <span class="time-l time pull-right">@r.User.Name</span>
                                            }
                                        </div>
                                    </div>
                                    <div class="pull-right w2">
                                        <div class="img">
                                            <img src="@System.Configuration.ConfigurationManager.AppSettings["httpimgpath"]@r.ImageFiles[0].FileName" class="imgyulan" />
                                        </div>
                                    </div>
                                }
                            </div>
                           
                        </li>
                    }
                </ul>
            }
            else
            {
                <ul class="Padding">
                    <li style="text-align:center;border:none;padding-top:40%; background-color:#ececec;"><img src="~/Images/weiyou.png" /></li>
                </ul>
            }

            @*已取消*@
            @if (ViewBag.RepairList_1 != null)
                                {
                <ul class="Padding">
                    @foreach (DoShineMP.Models.Repair r in ViewBag.RepairList_1 as List<DoShineMP.Models.Repair>)
                                {
                        <li id="@r.RepairId">
                            <div class="content-title-t">
                                @if (r.ImageFiles.Count == 0)
                                {
                                    <div class="pull-left w8 repaidid" tagid="@r.RepairId" onclick="repairid(@r.RepairId)">
                                        <p class="wr-content">@r.Contenet</p>
                                        <div class="content-title-b">
                                            <ins class="check check5 pull-left">已取消</ins>
                                            <span class="time pull-right">@r.CreateDate.ToString().Split(' ')[0]</span>
                                            @*@if (r.User.Name != null)
                                            {
                                                <span class="time-l time pull-right">@r.User.Name</span>
                                            }*@
                                        </div>
                                    </div>
                                    <div class="pull-right w2">
                                        <div class="img">
                                            <img src="~/Images/logo.png" class="imgyulan" />
                                        </div>
                                    </div>
                                }
                                else
                                {
                                    <div class="pull-left w8 repaidid" tagid="@r.RepairId" onclick="repairid(@r.RepairId)">
                                        <p class="wr-content">@r.Contenet</p>
                                        <div class="content-title-b">
                                            <ins class="check check5 pull-left">已取消</ins>
                                            <span class="time pull-right">@r.CreateDate.ToString().Split(' ')[0]</span>
                                            @*@if (r.User.Name != null)
                                            {
                                                <span class="time-l time pull-right">@r.User.Name</span>
                                            }*@
                                        </div>
                                    </div>
                                    <div class="pull-right w2">
                                        <div class="img">
                                            <img src="@System.Configuration.ConfigurationManager.AppSettings["httpimgpath"]@r.ImageFiles[0].FileName" class="imgyulan" />
                                        </div>
                                    </div>
                                }
                            </div>
                        </li>
                    }
                </ul>
            }
            else
            {
                <ul class="Padding">
                    <li style="text-align:center;border:none;padding-top:40%; background-color:#ececec;"><img src="~/Images/weiyou.png" /></li>
                </ul>
            }
        </div>
    </div>
</div>

@section scripts{
    <script src="http://res.wx.qq.com/open/js/jweixin-1.0.0.js"></script>
    <script src="~/Scripts/TouchSlide.1.1.js"></script>
    <script src="~/Scripts/zepto.min.js"></script>
    <script src="~/Scripts/cookiecode.js"></script>
    <script src="~/Scripts/locales/touch.js"></script>
    <script type="text/javascript">
        var tabnumber = 3;//分页tab数量
                                    var IndexLi = 0;

                                    var token;

                                    var images = {
                                        localId: [],
            serverId: []
                                    };
        $(function () {
            //read();
            $.post('@Url.Action("WechatJsConfigJson","PhoneWeb")', { url: window.location.href }, function (data, status) {
                var debug = data.debug;
                var appId = data.appId;
                var timestamp = data.timestamp;
                var nonceStr = data.nonceStr;
                var signature = data.signature

                wx.config({
                    debug: debug,
                    appId: appId,
                    timestamp: timestamp,
                    nonceStr: nonceStr,
                    signature: signature,
                    jsApiList: [
                        'checkJsApi',
                    'onMenuShareTimeline',
                    'onMenuShareAppMessage',
                    'onMenuShareQQ',
                    'onMenuShareWeibo',
                    'hideMenuItems',
                    'showMenuItems',
                    'hideAllNonBaseMenuItem',
                    'showAllNonBaseMenuItem',
                    'translateVoice',
                    'startRecord',
                    'stopRecord',
                    'onRecordEnd',
                    'playVoice',
                    'pauseVoice',
                    'stopVoice',
                    'uploadVoice',
                    'downloadVoice',
                    'chooseImage',
                    'previewImage',
                    'uploadImage',
                    'downloadImage',
                    'getNetworkType',
                    'openLocation',
                    'getLocation',
                    'hideOptionMenu',
                    'showOptionMenu',
                    'closeWindow',
                    'scanQRCode',
                    'chooseWXPay',
                    'openProductSpecificView',
                    'addCard',
                    'chooseCard',
                    'openCard'
                    ]
                });
            })

            var indexli = GetQueryString('IndexLi1');
            IndexLi = (indexli == window.localStorage['IndexLi1']) ? indexli : window.localStorage['IndexLi1'];
            TouchSlide({ slideCell: "#leftTabBox", defaultIndex: IndexLi });
            //console.log(IndexLi)
            //tab(IndexLi);
        })


        // 微信
        wx.error(function (res) {
            console.log('验证失败');
        });
        wx.ready(function () {
            // 5 图片接口
            // 5.1 拍照、本地选图
            var images = {
                localId: [],
                serverId: []
            };
            document.querySelector('#add-phoneimg').onclick = function () {
                wx.chooseImage({
                    success: function (res) {
                        images.localId = res.localIds;

                        if (images.localId.length == 0) {
                            alert('请先使用 chooseImage 接口选择图片');
                            return;
                        }
                        var i = 0, length = 0;
                        images.serverId = [];
                        function upload() {
                            wx.uploadImage({
                                localId: images.localId[i],
                                success: function (res) {
                                    i++;
                                    //alert('已上传：' + i + '张');
                                    images.serverId.push(res.serverId);
                                    $("#serviceid").val(res.serverId);
                                    //alert($("#serviceid").val())
                                    document.getElementById('add-phoneimg').src = res.localId;
                                },
                                fail: function (res) {
                                    alert(JSON.stringify(res));
                                }
                            });
                        }
                        upload();

                    }
                });
            };

        });

        $(document).on('click', '.imgyulan', function () {
            var imgurl = $(this).attr('src');
            //alert(imgurl)
            wx.previewImage({
                current: imgurl,
                urls: [
                 imgurl
                ]
            });
        })

        $('.hd li').click(function () {
            IndexLi = $(this).index();
            window.localStorage['IndexLi1'] = IndexLi;
            //tab(IndexLi);

        })


        $(window).swipeLeft(function () {
            document.body.scrollTop = 0;
            if (IndexLi < tabnumber - 1) {
                IndexLi++;
                window.localStorage['IndexLi1'] = IndexLi;
            }

        })
        console.log('初始化' + IndexLi)
        $(window).swipeRight(function () {
            document.body.scrollTop = 0;
            if (IndexLi > 0) {
                IndexLi--;
                window.localStorage['IndexLi1'] = IndexLi;
            }
        })


        function tab(enumber) {
            Lilength = $('.Padding').eq(enumber).find('li').length
            var windowHeight = $(window).height();
            LiHeight = 140;
            Height = (Lilength * LiHeight + 10) < (windowHeight - 50) ? (windowHeight - 50) : (Lilength * LiHeight + 10);
            $('.tempWrap .bd').css({ 'height': Height + 'px', 'overflow': 'hidden' })
        }


        function GetQueryString(name) {
            var reg = new RegExp("(^|&)" + name + "=([^&]*)(&|$)");
            var r = window.location.search.substr(1).match(reg);
            if (r != null) return unescape(r[2]); return null;
        }


        function repairid(e) {
            var repairid = $(this).attr('tagid');
            window.localStorage['IndexLi'] = IndexLi;
            window.history.replaceState(null, null, '?IndexLi1=' + window.localStorage['IndexLi1'] + '&openid=' + GetQueryString('openid'));
            var href = '@Url.Action("RepairDetails", "PhoneWeb")?repairid=' + repairid;
            window.location.href = '@Url.Action("RepairDetails", "PhoneWeb")?repairid=' + e;
        }

        function read() {
            if (window.localStorage['Historyread'] != null) {
                window.localStorage['Historyread'] == null;
                window.location.reload();
            }
        }

    </script>

}
